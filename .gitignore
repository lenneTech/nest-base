# Dependencies
node_modules/
bun.lockb.local

# Build output
dist/
build/
*.tsbuildinfo

# Generated artifacts
generated/
prisma/generated/
schema.prisma.merged

# Coverage / test reports
coverage/
reports/
.vitest-reports/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*

# Cache directories
.cache/
.turbo/
.parcel-cache/

# Claude / Ralph local files
.claude/ralph-loop.local.md

# Feature-gated Prisma migrations are materialised by `bun run prepare:schema`
# from `prisma/features/<feature>/migrations/` when the feature is enabled.
# The source location is tracked; the materialised copies are not.
prisma/migrations/20260428000200_postgis_extension/
prisma/migrations/20260428000250_geo_schema/
prisma/migrations/20260428000300_geo_gist_indexes/

# GeoIP database files — binary .mmdb downloads, never committed.
# `bun run scripts/download-geoip.ts` (or the `geoip-init` compose
# service) repopulates them from dbip-lite (CC-BY-4.0) or MaxMind
# (EULA + license_key). Attribution lives in README.md.
data/geoip/
*.mmdb
*.mmdb.gz

# OS-specific
Thumbs.db
